services:
  mongodb:
    image: mongo:7
    container_name: acme-finance-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: acme_finance_dw
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
